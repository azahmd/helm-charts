apiVersion: v1
kind: Service
metadata:
  name: {{ include "test-abc.fullname" . }}
  labels:
    {{- include "test-abc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "test-abc.selectorLabels" . | nindent 4 }}
