apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "test-abc.fullname" . }}-test-connection"
  labels:
    {{- include "test-abc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "test-abc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
